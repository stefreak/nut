{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "nut",
  "version": "0.1.0",
  "identifier": "com.nut.workspace-ui",
  "build": {
    "beforeDevCommand": "cd ../src-tauri && cargo run --bin generate-types && cd ../user-interface && pnpm i && pnpm run dev",
    "devUrl": "http://localhost:3000",
    "beforeBuildCommand": "cd ../src-tauri && cargo run --bin generate-types && cd ../user-interface && pnpm i && pnpm run build",
    "frontendDist": "../user-interface/build"
  },
  "bundle": {
    "active": false
  },
  "app": {
    "windows": [
      {
        "title": "nut",
        "width": 1200,
        "height": 800,
        "minWidth": 800,
        "minHeight": 600
      }
    ],
    "security": {
      "csp": null
    }
  }
}
